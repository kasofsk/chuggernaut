Create a file named `greeting.txt` at the repository root containing a short
friendly greeting (one or two lines). Commit the file with a descriptive
commit message and push the current branch to origin.

When you are done, call the `submit_result` tool on the chuggernaut-channel
MCP server with a one-line summary, then exit.
