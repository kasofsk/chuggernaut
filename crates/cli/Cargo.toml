[package]
name = "cli"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
types = { workspace = true, features = ["schema"] }
# For `schema api` only: the §6.2 request bodies typed in `api` rather than in
# `types`. The `chuggernaut` binary already links both crates, so this edge adds
# no dependency the shipped binary did not have.
api = { workspace = true, features = ["schema"] }
schemars.workspace = true
serde_yaml.workspace = true
store.workspace = true
auth.workspace = true
nkeys.workspace = true
vcs.workspace = true
anyhow.workspace = true
base64.workspace = true
chrono.workspace = true
clap.workspace = true
reqwest.workspace = true
serde_json.workspace = true
tokio.workspace = true

[dev-dependencies]
test-utils.workspace = true
tempfile.workspace = true

[lints]
workspace = true
